- [x] Create `public/phone.html` with a standard HTML5 boilerplate (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`)
- [x] Add `<meta name="viewport" content="width=device-width, initial-scale=1">` to the `<head>`
- [x] Add `<meta name="apple-mobile-web-app-capable" content="yes">` to suppress Safari chrome when added to home screen (optional but useful)
- [x] Add a `<style>` block that resets `margin`, `padding`, and `box-sizing` on `html` and `body`
- [x] Set `html` and `body` to `width: 100%; height: 100%; overflow: hidden` so there are no scrollbars
- [x] Set a dark `background-color` on `body` (e.g., `#111`)
- [x] Add a `<button id="arm-btn">` with text "Arm Camera"
- [x] Position `#arm-btn` absolutely, centred using `top: 50%; left: 50%; transform: translate(-50%, -50%)`
- [x] Apply `-webkit-tap-highlight-color: transparent` and `user-select: none` to `#arm-btn`
- [x] Add a `<div id="status">` element and set it to `display: none` in CSS (shown at bottom of screen on connect)
- [x] Add a `<script src="/socket.io/socket.io.js"></script>` tag before the closing `</body>`
- [x] Add an inline `<script>` that calls `const socket = io()` to connect to the server
- [x] Add a `console.log('Phone connected')` inside the script so connection can be confirmed in Web Inspector
- [x] Add `#portrait-overlay` element shown via CSS `@media (orientation: portrait)` — no JavaScript
- [x] On socket `connect`, show `#status` with text "Waiting for session to start…"
- [ ] Restart the server and visit `/phone` in the desktop browser — confirm the page renders without errors ← MANUAL: requires mkcert certs
- [ ] Check browser DevTools Network tab: confirm `/socket.io/socket.io.js` loads with HTTP 200 ← MANUAL: requires mkcert certs
- [ ] Confirm the server terminal logs "Client connected" when the phone page loads ← MANUAL: requires mkcert certs
- [ ] Open the page on the iPhone via the local IP URL and confirm the button is visible and centred ← MANUAL: requires physical device + mkcert certs
- [ ] Open Safari Web Inspector (via Mac → Develop menu) and confirm no console errors on the phone ← MANUAL: requires physical device + mkcert certs
